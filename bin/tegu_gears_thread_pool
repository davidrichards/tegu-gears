#!/usr/bin/env ruby

# This runs the thread pool on a machine.  It's best to run this with
# God, so that the process stays up and doesn't take over the server. 
# 
# Usage: ...
require File.dirname(__FILE__) + '/../lib/tegu_gears/thread_pool'

@tp = TeguGears::ThreadPool.new
@tp.process
